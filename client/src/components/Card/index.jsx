import React from "react";
import { Link } from "react-router-dom";

const Card = ({
  name,
  image,
  price,
  aux_images,
  description,
  discount,
  stock,
  rating,
  category,
}) => {
  return (
    <div>
      <h1></h1>
    </div>
  );
};

export default Card;
